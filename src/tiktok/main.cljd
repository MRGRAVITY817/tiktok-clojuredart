(ns tiktok.main
  (:require ["package:flutter/material.dart" :as m]
            [cljd.flutter :as f]))

(defn main []
  (f/run
   (m/MaterialApp
    .title "TikTok"
    .home
    (m/Scaffold
     .appBar (m/AppBar
              .title (m/Text "TikTok"))
     .body (m/Center
            .child (m/Text "Hello, TikTok!"))))))


